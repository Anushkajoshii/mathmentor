[project]
name = "backend"
version = "0.1.0"
description = "AI Math Mentor Backend with RAG and Multi-Agent System"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1",
    "groq>=0.11.0",
    "python-multipart>=0.0.12",
]
